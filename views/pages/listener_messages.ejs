<!DOCTYPE html>
<html lang="en">
    <div class="l_messages">
    <head>

    </head>
    <body>
        <main>
            <%- include('../partials/listener_navbar') %>
            <p>Messages</p>
            <% info.forEach(function (object) { %>
                <a href="/user/listener/<%= user.id %>/messages/<%= object.id %>"</a>><%= object.name %></a>
            <% }); %>
        </main>
    </body>
    </div>
</html>